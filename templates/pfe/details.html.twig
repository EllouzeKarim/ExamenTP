{% extends 'layout.html.twig' %}

{% block title %}Votre PFE{% endblock %}

{% block body %}

        <div class="card" >
            <h3 class="card-header">ID :{{ pfe.id }}</h3>
            <div class="card-body" >
                <h4 class="card-title">Titre PFE: {{ pfe.title }}</h4>
                <h4 class="card-title">Etudiant: {{ pfe.student }}</h4>
                <h6 class="card-subtitle mb-2 text-muted">Entreprise :{{ pfe.entreprise }}</h6>

            </div>

        </div>

{% endblock %}